on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say simple hello
    steps:
      - name: Checkout private tools
        uses: actions/checkout@v2
        with:
         repository: OKiMaureen/test
         path: .github/actions/test_action
