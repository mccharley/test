name: WorkflowB
on: [push]
jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - name: Sleep
        run: sleep 1m
  required-for-merge:
    name: "ptest"
    runs-on: ubuntu-latest
    needs:   foo  
    steps:
      - name: Fail
        env:
          DAY_OF_WEEK: "hellonmm"
        run: echo "${DAY_OF_WEEK}"
       
          
