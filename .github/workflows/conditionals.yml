name: Job Conditionals

on: workflow_dispatch

jobs:
  a:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello world"
  
  b:
   needs: a
   runs-on: ubuntu-latest

   steps:
    - run: echo "Hello world"
